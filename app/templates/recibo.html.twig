<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Recibo</title>
        <style>
            * {
                margin: 0;
                padding: 0;
            }

            body {
                font-family: Arial, Helvetica, sans-serif;
                margin: 2rem;
                line-height: 1.5;
            }
        </style>
    </head>
    <body>

        <div>
            <h1 style="margin-bottom: 1rem">Recibo</h1>

        </div>
        
        <div>
            <h3>
                <span style="color: #eb7100">{{ empresa.nome }}</span> <br />
                {{ empresa.endereco }} <br />
                {{ empresa.telefone }}
            </h3>
        </div>
       

        <hr style="margin: 1rem 0" />

        <div>
            <h4>Data: {{ data }}</h4>
            <p>Recebemos de: {{ nome }}</p>
            <p style="margin-bottom: 1rem">CPF: {{ cpf }}</p>
            <p>A importância de: R$ {{ valor|number_format(2, ',', '.') }}</p>
        </div>

        <hr style="margin: 1rem 0" />

        <div>
            <h4>Referente à:</h4>
            <table style="width: 100%; border-collapse: collapse; margin-bottom: 1rem; font-family: Arial, sans-serif;">
                <thead>
                    <tr>
                        <th style="text-align: left; padding: 8px; border-bottom: 2px solid #ccc; ">Quantidade</th>
                        <th style="text-align: left; padding: 8px; border-bottom: 2px solid #ccc"; >Item</th>
                        <th style="text-align: right; padding: 8px; border-bottom: 2px solid #ccc">Valor Unitário</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in carrinho %}
                    <tr>
                        <td style="padding: 8px; border-bottom: 1px solid #ccc;">{{ item.quantidade }}</td>
                        <td style="padding: 8px; border-bottom: 1px solid #ccc;">{{ item.nome }}</td>
                        <td style="text-align: right; padding: 8px; border-bottom: 1px solid #ccc;">R$ {{ item.preco }}</td>
                    </tr>
                    {% endfor %}
                    <!-- Linha do Frete -->
                    <tr>
                        <td></td>
                        <td style="padding: 8px; text-align: left; font-weight: bold; border-top: 2px solid #ccc;">Frete:</td>
                        <td style="text-align: right; padding: 8px; font-weight: bold; border-top: 2px solid #ccc;">R$ {{ frete|number_format(2, ',', '.')  }}</td>
                    </tr>
                    <!-- Linha do Valor Total -->
                    <tr>
                        <td></td>
                        <td style="padding: 8px; text-align: left; font-weight: bold;">Total:</td>
                        <td style="text-align: right; padding: 8px; font-weight: bold; ">R$ {{ valor|number_format(2, ',', '.')  }}</td>
                    </tr>
                </tbody>
            </table>

            <p >Valor Pago: R$ {{ valor|number_format(2, ',', '.') }}</p>
            <p>Valor Restante: R$ 0,00</p>

            <hr style="margin: 1rem 0" />

            <p>NOME DO FUNCIONÁRIO: Gilson Angelo Mangia</p>
            <p>Impresso em {{ data }} {{ hora }}</p>
        </div>

        <hr style="margin: 1rem 0" />
    </body>
</html>
